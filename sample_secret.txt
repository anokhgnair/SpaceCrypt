        self.copy_icon = tk.Button(encode_frame, text="📋", font=("Arial", 15), command=self.copy_encoded_text, width=2, height=1)
        self.copy_icon.grid(row=2, column=2, padx=8, pady=2, sticky="n")
    def copy_encoded_text(self):
        encoded = self.encoded_output_text.get("1.0", tk.END).rstrip("\n")
        if encoded:
            self.root.clipboard_clear()
            self.root.clipboard_append(encoded)
            self.root.update()  # now it stays on the clipboard after the window is closed
            messagebox.showinfo("Copied", "Encoded text copied to clipboard!")